<h1>Listing accounts</h1>

<table>
  <tr>
    <th>id</th>
    <th>email</th>
  </tr>

  <% @accounts.each do |account| %>
    <tr>
      <td><%= account.id %></td>
      <td><%= account.email %></td>
    </tr>
  <% end %>
</table>
